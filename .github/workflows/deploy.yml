name: Deploy to VPS

on:
  push:
    branches:
      - main # Cambia 'main' a tu rama principal si usas otra.

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    # 1. Checa el código
    - name: Checkout code
      uses: actions/checkout@v3

    # 2. Configurar acceso SSH
    - name: Set up SSH
      uses: webfactory/ssh-agent@v0.5.3
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    # 3. Sincroniza los archivos al VPS
    - name: Deploy Landing Page
      run: |
        ssh deploy@31.170.165.68"
          cd /var/www/eliconacento/landing-page &&
          git pull origin main &&
          pnmp install &&
          pnpm build &&
          pm2 restart landing-page
        "

    #- name: Deploy Medallones-Titanes
    #  run: |
    #    ssh deploy@<IP_VPS> "
    #      cd /var/www/eliconacento/medallones-titanes &&
    #      git pull origin main &&
    #      npm install &&
    #      pm2 restart medallones-titanes
    #    "
